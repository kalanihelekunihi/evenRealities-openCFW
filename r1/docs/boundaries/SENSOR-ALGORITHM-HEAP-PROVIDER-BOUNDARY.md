# Sensor-algorithm heap provider boundary

## Decision

The recovered application contains thirteen exact functions, totaling 1,202 executable bytes,
that implement a private heap used by linked sensor algorithms. The component is identified by
its `sensor_algo_mem_fatal, info1: %u` terminal diagnostic, its shared control-pointer slot at
`0x20007C64`, and the direct initialization edge from Goodix-candidate entry `0x0002A090`.

This evidence establishes one linked component, but not its author, source version, private API,
or license. The ownership ledger therefore records every body as
`unknown_sensor_algorithm_heap_provider_candidate` with disposition
`investigate_before_implementing`. It is implementation-blocked: OpenR1 does not translate these
bodies into C, silently substitute a different allocator, or treat their availability as
permission to reconstruct any sensor algorithm.

The exact census is generated by
`../../tools/summarize_r1_sensor_algorithm_heap.py`.

## Exact executable census

| Entry | Executable bytes | Evidence-only role |
| --- | ---: | --- |
| `0x0002D460` | 294 | validated free plus forward/backward coalescing |
| `0x0002D54C` | 116 | minimum-eight-byte allocation and requested-byte zeroing |
| `0x0002D5C0` | 152 | reallocation by grow/copy/free or in-place shrink/split |
| `0x0002E952` | 76 | `sensor_algo_mem_fatal` logging, flush, delay, and terminal loop |
| `0x00042D1C` | 6 | load the heap-control pointer stored at `0x20007C64` |
| `0x0006DFC8` | 4 | free tail-call thunk |
| `0x0006DFCC` | 40 | available tail-space query |
| `0x0006DFD6` | 124 | pool clearing and allocator initialization |
| `0x0006E004` | 4 | zeroing-allocation tail-call thunk |
| `0x00076A44` | 34 | unlink a free node and clear an empty-bin bitmap bit |
| `0x00093E14` | 38 | map adjusted block size into one of two bins |
| `0x00093E5E` | 68 | size-ordered circular free-list insertion |
| `0x000982C2` | 246 | bin search, remove, split, mark, and return payload |

Every body is length- and SHA-256-pinned by the summarizer and complete verifier. Five functions
are scatter-loaded; their Ghidra entry/end spans cross unrelated code, so the verifier hashes only
these executable spans in control-flow order:

- free: `0x0002D460..<0x0002D4D6` plus `0x00037480..<0x00037530`;
- fatal: `0x0002E952..<0x0002E964` plus `0x00092670..<0x000926AA`;
- available bytes: `0x0006DFCC..<0x0006DFD6` plus `0x0002D4D6..<0x0002D4F4`;
- initialize: `0x0006DFD6..<0x0006E004` plus `0x0002D4F4..<0x0002D542`; and
- allocation core: `0x000982C2..<0x00098340` plus `0x0003DF80..<0x0003DFF8`.

## Recovered data structure and behavior

The initializer receives fixed SRAM base `0x2003024C` from the Goodix-candidate initialization
path and a caller-supplied pool size. It rejects a null base, non-positive size, and pools of 1,024
bytes or less, clears the supplied pool, stores its base through `0x20007C64`, creates two circular
free-list sentinels, aligns the first block to eight bytes, and creates one tail block.

The recovered contract is:

- 44-byte allocator control prefix;
- two bitmap-selected, size-ordered free-list bins;
- minimum request of eight bytes and four-byte request rounding;
- eight-byte block header and low two status bits in the encoded size word;
- boundary records used for backward coalescing;
- split only when at least 16 bytes remain; and
- allocation failure routed into the terminal diagnostic rather than returned as ordinary
  recoverable pressure.

These details are compatibility evidence for licensed-provider integration and tests. They are not
an authorization to reproduce the allocator.

## Excluded source matches

This component is not FreeRTOS `heap_4`. The application's actual authenticated FreeRTOS-Kernel
10.0.0 `heap_4` is separately source-routed and uses its characteristic single address-ordered
free list, allocated-bit rules, scheduler suspension, and 51,200-byte heap. The six-byte body at
`0x00042D1C` happens to share instruction bytes with `xTaskGetCurrentTaskHandle`, but it loads
`0x20007C64`, not `pxCurrentTCB` at `0x2000781C`; identical generic accessor bytes do not establish
common ownership.

It is also not TLSF v3.1. The pinned Matthew Conte TLSF source elsewhere in this repository uses
the substantially larger two-level segregated-fit control structure and mapping rules. The R1
component instead has a 44-byte control prefix and only two bins. No TLSF source is admitted for
this boundary.

## Integration rule

A matching licensed provider package may replace this gate only after its source or binary,
version, license, ABI, allocation callbacks, fatal policy, and function-level relationship to the
Goodix and other sensor algorithms are authenticated. If a licensed algorithm SDK accepts host
allocation callbacks, OpenR1 may provide an attributable allocator through that published API,
but must not claim byte-equivalence or reproduce this unidentified implementation. Until then,
the dependent sensor-algorithm path remains disabled and returns provider-unavailable behavior.

The parser is read-only. It performs no allocation, hardware access, firmware mutation, or code
signing operation.
