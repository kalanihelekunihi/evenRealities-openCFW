# Sensor-algorithm heap provider boundary

## Decision

The recovered application contains thirteen exact functions, totaling 1,202 executable bytes,
that implement a private heap used by linked sensor algorithms. The component is identified by
its `sensor_algo_mem_fatal, info1: %u` terminal diagnostic, its shared control-pointer slot at
`0x20007C64`, and the direct initialization edge from Goodix-candidate entry `0x0002A090`.

Provenance is resolved. The component is Goodix's `goodix_mem` / `GdMem` memory-pool manager
from the Goodix GH3X2X health-sensor algorithm SDK common DSP support library; R1 carries the
SDK config tag `gh3x2x-v2.23_7ecd2a`, matching the public GH3X2X SDK mirror. The public SDK
header `goodix_mem.h` declares `goodix_mem_init`/`deinit`/`malloc`/`free`/`get_free_size` with
exactly the -1 (null base) / -2 (non-positive size) error contract the R1 init body implements,
and declares `extern void Gh3x2xPoolIsNotEnough(void)` as an integrator-supplied callback.
Instruction-level disassembly comparison against the Goodix common-DSP library object matched
the R1 members: `GdMemInit` (second span of `0x0006DFD6`: 1,024-byte minimum pool, sentinel
`strd` pairs, +0x33&~7 alignment, size<<2 tail encoding), `goodix_mem_init` (null→-1,
size≤0→-2, memset, tail-call `GdMemInit`), `GdMemMalloc` (minimum-8 request, round-4, +8
header, bin split at 1,024, bitmap search, split-if->15, |3 mark, top-chunk fallback,
`Gh3x2xPoolIsNotEnough` on failure), `GdMemFree` (`0x0002D460`), `GdMemRealloc` (`0x0002D5C0`),
unlink (`0x00076A44`), insert (`0x00093E5E`), and `GdMemGetFreeSize` (`0x0006DFCC`).

The Goodix SDK license is restrictive (BSD-style, use limited to Goodix ICs, no reverse
engineering of binary forms) and the module ships binary-only. The ownership ledger therefore
routes the allocator internals and the Goodix consumer call-site glue to
`goodix_gh3x2x_candidate` with disposition `vendor_source_required_not_redistributable`, the
same gate as the rest of the GH3X2X boundary. The component was formerly censused under
`unknown_sensor_algorithm_heap_provider_candidate`. It remains implementation-blocked: OpenR1
does not translate these bodies into C, silently substitute a different allocator, or treat
their availability as permission to reconstruct any sensor algorithm.

Two members are integrator-authored R1 glue, not Goodix code, and are the only locally
implementable behavior: `0x0002E952`, the integrator-supplied `Gh3x2xPoolIsNotEnough` handler
(logs `sensor_algo_mem_fatal, info1: %u`, flushes, then loops forever), and `0x00092B60`, the
12-byte product byte-fill (memset) used by `goodix_mem_init` for pool clearing. Both route to
`r1_product_specific` / `clean_room_behavior_only`.

The exact census is generated by
`../../tools/evidence/summarize_r1_sensor_algorithm_heap.py`.

## Exact executable census

| Entry | Executable bytes | Evidence-only role | Routing |
| --- | ---: | --- | --- |
| `0x0002D460` | 294 | validated free plus forward/backward coalescing (`GdMemFree`) | Goodix vendor gate |
| `0x0002D54C` | 116 | minimum-eight-byte allocation and requested-byte zeroing (`GdMemMalloc` entry) | Goodix vendor gate |
| `0x0002D5C0` | 152 | reallocation by grow/copy/free or in-place shrink/split (`GdMemRealloc`) | Goodix vendor gate |
| `0x0002E952` | 76 | `sensor_algo_mem_fatal` logging, flush, delay, and terminal loop (`Gh3x2xPoolIsNotEnough`) | R1 integrator glue |
| `0x00042D1C` | 6 | load the heap-control pointer stored at `0x20007C64` | Goodix vendor gate |
| `0x0006DFC8` | 4 | free tail-call thunk (`goodix_mem_free`) | Goodix vendor gate |
| `0x0006DFCC` | 40 | available tail-space query (`GdMemGetFreeSize`) | Goodix vendor gate |
| `0x0006DFD6` | 124 | pool clearing and allocator initialization (`goodix_mem_init` + `GdMemInit`) | Goodix vendor gate |
| `0x0006E004` | 4 | zeroing-allocation tail-call thunk (`goodix_mem_malloc`) | Goodix vendor gate |
| `0x00076A44` | 34 | unlink a free node and clear an empty-bin bitmap bit | Goodix vendor gate |
| `0x00093E14` | 38 | map adjusted block size into one of two bins | Goodix vendor gate |
| `0x00093E5E` | 68 | size-ordered circular free-list insertion | Goodix vendor gate |
| `0x000982C2` | 246 | bin search, remove, split, mark, and return payload (`GdMemMalloc` core) | Goodix vendor gate |

## Call-site glue census

Twenty-one further guarded alloc/free bodies from the sub-32-byte frontier complete the
34-row boundary. Twenty are call-site glue inside already Goodix-gated consumer closures and
route to the Goodix vendor gate with the calling provider; `0x00092B60` is the R1 product
byte-fill described above.

| Entry | Executable bytes | Evidence-only role | Caller context |
| --- | ---: | --- | --- |
| `0x00028EAC` | 20 | guarded free of `*p` then null | Goodix teardown `0x0007CBA0` |
| `0x00028EC0` | 10 | teardown tail-branch step | Goodix session teardown `0x0006EB30` |
| `0x0002963A` | 28 | allocate 0x40 via zero-allocate, memclr | Goodix buffer-pool init `0x00096A20` |
| `0x00034A3C` | 28 | allocate 0x18 arrays into record +8/+0x18 | Goodix `0x0006EB94` |
| `0x00036230` | 12 | guarded free returning 0 | Goodix context destroy `0x0003E6B0` |
| `0x00036C60` | 26 | context destroy: free `*(p+0x300)` then `p` | via glue `0x00028EC0` |
| `0x0003757C` | 12 | guarded free returning 0 | no resolved caller; wraps `GdMemFree` between Goodix-gated neighbors |
| `0x00056860` | 20 | guarded free of `*p` via free thunk | exclusive Goodix caller `0x0006DAD0` |
| `0x00066276` | 20 | guarded free-and-null helper | Goodix `0x00093E3A` |
| `0x0006628A` | 20 | guarded free-and-null helper | Goodix `0x00029BBC` plus glue `0x00073154` |
| `0x0006629E` | 20 | guarded free-and-null helper | Goodix teardown `0x0006CC60` |
| `0x000662B2` | 20 | guarded free-and-null helper | Goodix `0x00091890` |
| `0x000662C6` | 20 | guarded free-and-null helper | Goodix `0x00029BBC`/`0x000304A0`/`0x000305D8`/`0x00033800`/`0x0006CC60`/`0x00091890` |
| `0x000662EA` | 26 | buffer-descriptor init, zero-allocate n<<2 | Goodix `0x0003727C` |
| `0x00066304` | 26 | buffer-descriptor init, zero-allocate n<<1 | Goodix `0x00031914`/`0x00072C48` |
| `0x0006631E` | 28 | buffer-descriptor init, zero-allocate n | Goodix `0x0006D204` |
| `0x000667C0` | 6 | tail free of `*(p+4)` | exclusive Goodix caller `0x00029BBC` |
| `0x00073154` | 22 | free record fields +4/+0x10 via `0x0006628A` | Goodix teardown `0x00037E8A` |
| `0x00092B58` | 2 | 2-byte thunk into R1 memset `0x00092B60` | sole caller `goodix_mem_init` |
| `0x00092B60` | 12 | product byte-fill (memset) for pool clearing | R1 integrator glue |
| `0x00098FFC` | 20 | free fields +8/+0x18 via `GdMemFree` | Goodix session teardown `0x0006EB30` |

Every body is length- and SHA-256-pinned by the summarizer and complete verifier. Five functions
are scatter-loaded; their Ghidra entry/end spans cross unrelated code, so the verifier hashes only
these executable spans in control-flow order:

- free: `0x0002D460..<0x0002D4D6` plus `0x00037480..<0x00037530`;
- fatal: `0x0002E952..<0x0002E964` plus `0x00092670..<0x000926AA`;
- available bytes: `0x0006DFCC..<0x0006DFD6` plus `0x0002D4D6..<0x0002D4F4`;
- initialize: `0x0006DFD6..<0x0006E004` plus `0x0002D4F4..<0x0002D542`; and
- allocation core: `0x000982C2..<0x00098340` plus `0x0003DF80..<0x0003DFF8`.

## Recovered data structure and behavior

The initializer receives fixed SRAM base `0x2003024C` from the Goodix-candidate initialization
path and a caller-supplied pool size. It rejects a null base, non-positive size, and pools of 1,024
bytes or less, clears the supplied pool, stores its base through `0x20007C64`, creates two circular
free-list sentinels, aligns the first block to eight bytes, and creates one tail block.

The recovered contract is:

- 44-byte allocator control prefix;
- two bitmap-selected, size-ordered free-list bins;
- minimum request of eight bytes and four-byte request rounding;
- eight-byte block header and low two status bits in the encoded size word;
- boundary records used for backward coalescing;
- split only when at least 16 bytes remain; and
- allocation failure routed into the terminal diagnostic rather than returned as ordinary
  recoverable pressure.

These details are compatibility evidence for licensed-provider integration and tests. They are not
an authorization to reproduce the allocator.

## Excluded source matches

This component is not FreeRTOS `heap_4`. The application's actual authenticated FreeRTOS-Kernel
10.0.0 `heap_4` is separately source-routed and uses its characteristic single address-ordered
free list, allocated-bit rules, scheduler suspension, and 51,200-byte heap. The six-byte body at
`0x00042D1C` happens to share instruction bytes with `xTaskGetCurrentTaskHandle`, but it loads
`0x20007C64`, not `pxCurrentTCB` at `0x2000781C`; identical generic accessor bytes do not establish
common ownership.

It is also not TLSF v3.1. The pinned Matthew Conte TLSF source elsewhere in this repository uses
the substantially larger two-level segregated-fit control structure and mapping rules. The R1
component instead has a 44-byte control prefix and only two bins. No TLSF source is admitted for
this boundary.

## Integration rule

The provider is identified but its license does not permit redistribution or reconstruction:
the Goodix GH3X2X SDK is BSD-style with use limited to Goodix ICs, bars reverse engineering of
binary forms, and ships this module binary-only. A licensed Goodix provider package may replace
this gate only after its source or binary, version, license, ABI, allocation callbacks, fatal
policy, and function-level relationship to the other sensor algorithms are authenticated. If a
licensed algorithm SDK accepts host allocation callbacks, OpenR1 may provide an attributable
allocator through that published API, but must not claim byte-equivalence or reproduce this
vendor implementation. The integrator-supplied split is explicit in the public header:
`Gh3x2xPoolIsNotEnough` is the product's own fatal policy (`0x0002E952`), and the pool-clearing
byte-fill (`0x00092B60`) is ordinary R1 product code; only those two bodies are clean-room
candidates. Until a licensed provider is admitted, the dependent sensor-algorithm path remains
disabled and returns provider-unavailable behavior.

The parser is read-only. It performs no allocation, hardware access, firmware mutation, or code
signing operation.
