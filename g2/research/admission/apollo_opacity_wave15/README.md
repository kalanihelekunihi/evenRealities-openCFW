# Apollo opacity Wave 15 boundary

Wave 15 closes the 1,668-byte residual root at `0x0051B8F0`. All 20 real
Thumb `BL` sites target nine functions already typed in Waves 1, 2, 3, and 6.
Eight apparent `func_0x...` calls are MVE decompiler artifacts with no machine
branch site.

The root is the paired cap/fan coordinator immediately after Wave 10's round
join coordinator. Its control-state, trigonometric, square-root, tessellation,
and command-record topology is exact, but checked-in evidence does not expose
the maintained internal NemaVG implementation or license. It is therefore a
SHA-pinned typed unavailable boundary, not a fabricated source identity.

One four-byte interior literal, the prior Wave-10 shared context pointer, and
the four-byte `180.0f` literal immediately before the sibling root are fully
accounted. No production route or hardware operation is performed.
