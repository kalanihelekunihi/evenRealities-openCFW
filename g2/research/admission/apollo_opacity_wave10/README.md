# Apollo opacity wave 10 boundary

This software-only record closes the complete actionable static closure rooted
at `0x0051B140`: one function and 1,962 official opaque bytes. The installed
Thumb body contains exactly 26 direct `BL` sites, all targeting seven functions
already typed in waves 1, 2, or 6. It has no `B.W` call continuation and no
register-`BLX` site.

Ghidra renders sixteen operations in repeated MVE/Helium blocks as call-like
`func_0x...` expressions. None of those identifiers is a machine-code branch
target. They are exhaustively recorded as decoder artifacts with zero bytes,
not invented as external functions or providers.

The body's two decoded ranges omit a referenced four-byte `180.0f` literal
already included in its official envelope. The six-byte census gap immediately
after the envelope contains two padding bytes and a second referenced `180.0f`.
A four-byte command-context pointer is shared with the next residual function.
These physical support intervals are SHA-pinned and add no function-opacity
bytes.

The body tessellates a rounded stroke join into a point fan and emits it through
the prior vector-path helpers. That bounded behavior and topology support only
NemaGFX/NemaVG family context: no checked-in evidence proves an exact maintained
upstream function or license. The row therefore remains
`typed-external-provider-unavailable`. No callable replacement, production
overlay, hardware operation, signing, flashing, or probing is performed.
