# Apollo opacity wave 4 boundary

This research-only record closes the complete no-evidence call graph rooted at
`0x0043A698`. Twenty positive-opaque rows account for 8,116 official bytes;
eight additional reachable rows already carry zero official opaque bytes.

The zero-byte distinction is explicit. `0x00439BE4` is inside the authenticated,
source-recreated IAR `memcpy` span. The other seven are callable interior rows
whose physical bytes are already charged to the overlapping 1,526-byte
`0x0043BA98` envelope; zero does not independently prove source ownership.

Positive evidence bounds the only ingress to `0x0055F848`, which the nPMX
provider audit classifies as an Even first-party orientation/calibration seam
and explicitly excludes from Nordic nPMX. It does not identify the linked
numerical algorithm's original provider, source, or license. Eighteen rows
therefore remain generic typed external boundaries. The one exact provider
exception is the split `[0x0043C0E4,0x0043C14A)` IAR DLIB `memset`-family
entry and continuation, repeatedly authenticated as one 102-byte provider
span; its proprietary source remains unavailable.

No callable implementation is supplied and nothing is production-routed.
