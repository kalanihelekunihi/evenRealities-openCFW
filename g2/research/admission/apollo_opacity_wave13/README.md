# Apollo opacity wave 13

This research-only admission closes the complete residual static and indirect
LTPF graph rooted at `0x00438FB8`. Ten corpus functions account for 4,016
official opaque bytes and map positively to the authenticated Google liblc3
v1.1.3-compatible `ltpf.c` baseline under Apache-2.0. The linked `memmove`
body is reconciled at zero official bytes and remains an unavailable IAR DLIB
boundary.

The root's seven-slot `resample_12k8[]` table is part of the actionable graph,
not merely data decoration. Four residual targets and their shared helper are
included in the 4,016-byte result. The 16 kHz and 48 kHz targets are absent
from `functions.jsonl`; `non_corpus_boundaries.tsv` records their exact entry
starts and bounded physical SHA-256 values without claiming that the next-entry
bounds prove exact compiler function extents. They add zero official bytes.

Eight private coefficient/interpolation tables match the public snapshot
byte-for-byte. Eleven omitted Ghidra ranges (46 bytes) are exhaustively pinned;
all are already inside selected official envelopes and add no bytes.

The public snapshot is a reproducible compatibility baseline, not proof of the
private generating checkout. Production routing remains excluded until the
exact checkout/configuration, ABI, and reviewed dual-profile Cortex-M55
codegen/relocation/link/placement receipts exist.
