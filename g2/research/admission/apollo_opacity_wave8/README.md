# Apollo opacity wave 8 admission record

This research-only record closes the complete static actionable graph rooted
at `0x005A8D06`: 28 positive-byte functions (5,370 bytes), one reachable
physical function already counted as zero official opaque bytes, six terminal
functions already classified by earlier work, and every direct `DAT_` support
reference emitted by the 29 reachable bodies.

Twenty positive-byte rows (2,552 bytes) are positively attributable to exact
FreeType 2.9.1 functions in the authenticated, FTL-licensed snapshot.  The
remaining eight positive rows (2,818 bytes), plus the zero-byte census row,
remain typed fail-closed boundaries because no exact maintained provider source
and license are available.  Exact source identity is a source-level admission,
not a claim of byte-identical IAR code generation.

`shared_data.tsv` pins the three FreeType discriminators (`0x0001FB66`,
`"font-format"`, and `"Type 1"`) and the root's complete 250-record
Unicode-sequence data graph.  The table sentinel is `0x1469`, exactly the byte
length of the UTF-8 pool, and every referenced offset ends in a valid
NUL-terminated UTF-8 string.  Support data are outside function-opacity
accounting and add zero bytes.

Nothing here is routed into a production overlay or performs hardware work.
