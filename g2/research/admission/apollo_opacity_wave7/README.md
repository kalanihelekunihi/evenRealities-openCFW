# Apollo opacity wave 7 admission record

This research-only record closes the two-function actionable graph rooted at
`0x00564974`, totaling 4,784 official opaque bytes.

The function corpus excludes four embedded NOP/literal islands from its code
range byte counts. `reconciled_interiors.tsv` pins all 22 physical bytes and
shows that they are already covered by the owning envelopes. The context
pointer at `0x00564A40` is shared by both functions; none of the four islands
adds a function or any new wave-7 byte count.

No exact maintained source/provider/license identity is authenticated. Both
functions remain SHA-pinned `typed-external-provider-unavailable` boundaries,
with no production or hardware route.
