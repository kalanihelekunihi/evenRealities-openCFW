# Apollo opacity wave 5 admission record

This research-only record closes the largest remaining positive-opaque Apollo
envelope after wave 4. The root at `0x00519290` implements bounded cubic-vector
subdivision and stroke/path state handling. Its exact upstream function,
provider, source body, and license are not authenticated by the checked-in
evidence, so it remains a SHA-pinned `typed-external-provider-unavailable`
boundary.

`reconciled_graph.tsv` proves that every direct call leaves the actionable
wave-4 residual: twelve targets were typed by waves 1–3, and the thirteenth is
the zero-official-opaque, source-recreated IAR `sqrtf`. The prior positive byte
counts are recorded for clarity but contribute zero new wave-5 bytes.

The analyzer and tests are read-only. Nothing in this directory is routed into
the production overlay, linked image, signing flow, or hardware tooling.
