# Apollo opacity wave 3 boundary

This research-only record closes the complete unresolved call closure rooted at
`0x00517E18`: fifteen functions / 7,672 official opaque bytes across closure
depths zero through five. The closure stops only at previously typed rows,
parent-classified LVGL/heap rows, and authenticated IAR runtime entries.

The root envelope contains 5,224 official bytes and 5,170 decoded corpus bytes.
Its 54-byte shortfall stays inside the SHA-pinned external envelope. Every
other selected body is complete in the corpus.

The bodies are consistent with vector-path, geometry, command-record, and
buffer-management work. The authenticated Ambiq/Nema package remains relevant
family context, but its eleven resolved stock symbols do not include any of
these fifteen entries. The public Apollo5 archive uses GCC and is not the IAR
stock-generating archive. No exact upstream function identity or license is
therefore assigned.

All rows are `typed-external-provider-unavailable`. They provide no callable
implementation and are not production-routed. Stock must be retained or the
boundary must fail closed until exact provider, ABI, source, license, and
placement evidence becomes available.
