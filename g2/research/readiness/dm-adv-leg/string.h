#ifndef OPENCFW_ATTS_CSF_PROBE_STRING_H
#define OPENCFW_ATTS_CSF_PROBE_STRING_H

#include <stddef.h>

void *memcpy(void *restrict dst, const void *restrict src, size_t n);
void *memset(void *dst, int value, size_t n);

#endif
