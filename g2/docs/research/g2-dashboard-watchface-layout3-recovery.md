# G2 dashboard watchface layout 3 recovery

The single 1,708-byte retained-path anchor expands to nineteen functions /
3,254 body bytes in `[0x005B9CEC,0x005BAB2C)`, a 3,648-byte physical object.
Seven source-order lifecycle/widget routines were restored beyond Ghidra.

All 173 external calls terminate at admitted LVGL (125), EasyLogger (20), and
mpaland printf (2), bounded IAR DLIB (10), or first-party dashboard providers
(16). The object reuses commits `344c7c318…`, `a596b264…`, and `d3b98468…`;
it embeds no utility implementation and adds no version/private-commit signal.

Ingress closes over 28 BL sites and nine stored pointers. Five raw BL
lookalikes at `0x004CA3A4`, `0x004CA44E`, `0x004CA46A`, `0x004CA4C8`, and
`0x004CA4E4` each begin on the second halfword of a valid four-byte `sdiv`, so
none is strict-interior ingress. Remaining work is first-party recreation and
display validation; the object is not production-routed.
