# G2 bootloader semaphore create `0x00416762` source closure

The complete 180-byte body `[0x00416762,0x00416816)` hashes to
`48c3940e6f762adfbdbef5d40813bfcc043edd10947ad93982c1b8c1051a410f`;
its sole direct caller at `0x004305E0` is authenticated.

`runtime_semaphore_create_416762.c` preserves critical/argument guards, the
exact dynamic or 80-byte static control-storage contract, binary versus
counting semaphore constructors, initial-token give, and delete-on-give-
failure behavior. Apple and Linux each emit 140 bytes under seven strict
relocations. Six host tests, both target profiles, provider/manifest routing,
and both unsigned packages pass offline. Live scheduler/object behavior is
blocked by unavailable authorized responsive G2 physical evidence.
