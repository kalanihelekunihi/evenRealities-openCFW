# G2 universal-setting service recovery

The five retained-path anchors expand to fifteen functions / 2,010 body bytes
in `[0x00466010,0x0046687C)`, a 2,156-byte physical object. Ten source-order
helpers missed by Ghidra restore the live-setting accessor, CRC refresh,
record comparator, message handler, and six field getters. Eighty-one
whole-image BL sites reach exact starts. One authenticated BL reaches an
instruction interior and two raw unaligned words collide with interiors; no
stored function pointer or indirect call exists.

All 103 external calls terminate at admitted EasyLogger (75), bounded IAR
memory primitives (17), the production source-owned CCITT-FALSE provider (3),
already closed KV record writers (3), or first-party role/sync/scheduling
providers (5). The object adds no reusable implementation, version signal, or
observable private producing commit. Remaining work is first-party source
recreation and hardware settings/synchronization validation. Reproduce with
`python3 tools/analyze_g2_service_universal_setting.py` and its focused test.
