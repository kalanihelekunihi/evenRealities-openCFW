--- a/fmod.c
+++ b/fmod.c
@@ -13,11 +13,15 @@
 	/* float load/store to inner loops ruining performance and code size */
 	uint64_t uxi = ux.i;
 
-	if (uy.i<<1 == 0 || isnan(y) || ex == 0x7ff)
-		return (x*y)/(x*y);
+	if (uy.i<<1 == 0 || (ey == 0x7ff && (uy.i<<12) != 0) || ex == 0x7ff) {
+		ux.i = 0x7ff8000000000000ULL;
+		return ux.f;
+	}
 	if (uxi<<1 <= uy.i<<1) {
-		if (uxi<<1 == uy.i<<1)
-			return 0*x;
+		if (uxi<<1 == uy.i<<1) {
+			ux.i = (uint64_t)sx << 63;
+			return ux.f;
+		}
 		return x;
 	}
 
@@ -41,16 +45,20 @@
 	for (; ex > ey; ex--) {
 		i = uxi - uy.i;
 		if (i >> 63 == 0) {
-			if (i == 0)
-				return 0*x;
+			if (i == 0) {
+				ux.i = (uint64_t)sx << 63;
+				return ux.f;
+			}
 			uxi = i;
 		}
 		uxi <<= 1;
 	}
 	i = uxi - uy.i;
 	if (i >> 63 == 0) {
-		if (i == 0)
-			return 0*x;
+		if (i == 0) {
+			ux.i = (uint64_t)sx << 63;
+			return ux.f;
+		}
 		uxi = i;
 	}
 	for (; uxi>>52 == 0; uxi <<= 1, ex--);
--- a/fmodf.c
+++ b/fmodf.c
@@ -10,11 +10,15 @@
 	uint32_t i;
 	uint32_t uxi = ux.i;
 
-	if (uy.i<<1 == 0 || isnan(y) || ex == 0xff)
-		return (x*y)/(x*y);
+	if (uy.i<<1 == 0 || (ey == 0xff && (uy.i<<9) != 0) || ex == 0xff) {
+		ux.i = 0x7fc00000U;
+		return ux.f;
+	}
 	if (uxi<<1 <= uy.i<<1) {
-		if (uxi<<1 == uy.i<<1)
-			return 0*x;
+		if (uxi<<1 == uy.i<<1) {
+			ux.i = sx;
+			return ux.f;
+		}
 		return x;
 	}
 
@@ -38,16 +42,20 @@
 	for (; ex > ey; ex--) {
 		i = uxi - uy.i;
 		if (i >> 31 == 0) {
-			if (i == 0)
-				return 0*x;
+			if (i == 0) {
+				ux.i = sx;
+				return ux.f;
+			}
 			uxi = i;
 		}
 		uxi <<= 1;
 	}
 	i = uxi - uy.i;
 	if (i >> 31 == 0) {
-		if (i == 0)
-			return 0*x;
+		if (i == 0) {
+			ux.i = sx;
+			return ux.f;
+		}
 		uxi = i;
 	}
 	for (; uxi>>23 == 0; uxi <<= 1, ex--);
